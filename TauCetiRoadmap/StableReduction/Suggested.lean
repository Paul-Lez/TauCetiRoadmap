import Mathlib

/-!
# Stable reduction of curves: target signatures

**This file is not the roadmap and is not exhaustive.** The definitive document is
`README.md`. The statements here will suggest Lean forms for particular milestones so that
contributors and reviewers converge on names and signatures; discharging all of them will
finish neither a layer nor the roadmap.

There are no compiled target signatures yet. At the pinned Mathlib commit, the vocabulary
needed to state them faithfully does not exist: families of curves, at-worst-nodal morphisms,
relative dualizing sheaves, stable pointed families, models over a DVR, and finite DVR
extensions are all roadmap targets. Introducing placeholder `Prop` definitions here would
make the summit vacuous, so signatures will be added only as those types land in Tau Ceti.

The narrative roadmap fixes the mathematical statements, conventions, proof route,
dependency layers, acceptance examples, and boundary with stable maps.
-/

namespace TauCetiRoadmap.StableReduction

-- No compiled targets yet; see README.md.

end TauCetiRoadmap.StableReduction
